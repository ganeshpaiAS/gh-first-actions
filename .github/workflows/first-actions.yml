name: First Worflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
          echo "Hello World"
          echo "First Job"
      - name: Done by
        run: echo "Done - Bye"
